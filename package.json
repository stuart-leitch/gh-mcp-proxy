{
  "name": "github-mcp-worker",
  "version": "0.1.0",
  "private": true,
  "description": "CloudFlare Worker exposing GitHub via MCP, authed with a fine-grained PAT.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
